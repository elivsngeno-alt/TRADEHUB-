import React from 'react';
import AdminPanelPage from '../premium/pages/AdminPanelPage';

/**
 * AdminDashboard component
 * Fetches and displays user sign-in data and commission earnings with administrative access gating.
 */
export const AdminDashboard: React.FC = () => {
    return <AdminPanelPage />;
};

export default AdminDashboard;
